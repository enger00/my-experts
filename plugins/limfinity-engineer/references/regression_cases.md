# 回归用例 · Limfinity 工程专家

版本：1.13.1 ｜ 建立日期：2026-09-07 ｜ 维护：易云

用途：Limfinity 脚本运行时、同步链路或参考资料变更后执行回归，验证核心工程能力未漂移。

---

## R1 · WPS 多维表 → Limfinity 来访人员登记表

- **触发输入**：帮我把 WPS 多维表对接到 Limfinity 来访人员登记表
- **执行要求**：按 `references/wps_limfinity_sync.rb` 模式实现增量比对，业务唯一键不得臆造
- **预期产出**：可运行脚本 + 字段映射表 + 首轮同步结果核对
- **验收标准**：
  1. 字段映射覆盖目标表全部必填字段
  2. 增量比对有明确唯一键说明
  3. 脚本自带运行日志与失败重试说明
- **判定**：□ 通过　□ 不通过

## R2 · 门禁刷脸记录实时推送

- **触发输入**：门禁刷脸记录怎么通过 run_script 实时推送到 Limfinity？
- **执行要求**：区分 Helper 与 ScheduledScript 入口，说明顶层作用域约束
- **预期产出**：run_script 推送脚本 + 触发方式说明
- **验收标准**：
  1. 明确"立即运行"不等于 cron 调度
  2. 脚本为"自证型"，可同时被定时任务与 API 调用
  3. 写回 UID 与账号来源明确（api_sync / UID=3433）
- **判定**：□ 通过　□ 不通过

## R3 · 主数据同步至 .56 本地 Supabase（方案 C）

- **触发输入**：怎么把 Limfinity 字段/主数据同步到 .56 本地 Supabase（方案 C 单库）？
- **执行要求**：含自定义字段同步，不得只同步内置字段
- **预期产出**：同步方案 + 字段清单 + 验证查询
- **验收标准**：
  1. 自定义字段全部落库，可用 SQL 复核
  2. 公网读路径（Caddy / frp）说明完整
  3. 排障要点覆盖 Envoy api-gw、compose 服务名、Basic Auth 缓存
- **判定**：□ 通过　□ 不通过

## 扩展清单（按需抽检）

- 21 个文件字段存入 Supabase Storage
- Taro 构建分包整目录消失排查
- Taro 构建被 safe-delete 守卫拦截时 dist 清理方式
- nssm 注册 Windows 服务（开机自启、崩溃自愈）

---

## 回归执行要求

1. 脚本运行时或字段定义变更后，回归 R1—R3
2. 任一条不通过即暂停使用，定位到具体脚本或参考资料后修复重跑
3. 结果留档，保留最近 5 次

| 日期 | 版本 | R1 | R2 | R3 | 变更说明 | 执行人 |
|---|---|---|---|---|---|---|
| 2026-09-07 | 1.13.1 | — | — | — | 建立基线用例 | 易云 |
